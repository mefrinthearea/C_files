#include <stdio.h>

int main(){
  //constant = fixed value that cannnot be altered by the program during its execution

  const float PI = 3.14159;

  // if it try to assign some kind of new value to PI. it wont work. PI=6 for example. NO 
  
  printf("pi is %f\n", PI);
}